import numpy as np
N = int(input( ))
M = int(input( ))
i = int(input( ))
j = int(input( ))

A = np.ndarray(shape = (N,M))
B = np.ndarray(shape = (N,M))
C = np.ndarray(shape = (N,M))

for i in range:
  for j in range:
    if A[i][j] > B[i][j]:
      C[i][j] = A[i][j]
    else:
      C[i][j] = B[i][j]